export default function PlanLibrary({ plans, onLoad, onDelete }) {
  if (!plans.length) return null;
  return <section className="plan-library shell" aria-label="Plan library"><div className="library-heading"><div><p>YOUR PRIVATE ARCHIVE</p><h2>Every route,<br /><span className="tone-muted">kept close.</span></h2></div><span>{plans.length} plans stored on this device</span></div><div className="library-rail">{plans.map((plan, index) => <article key={plan.id} className="library-card"><div className="library-card-top"><span>{String(index + 1).padStart(2, '0')}</span><button onClick={() => onDelete(plan.id)} aria-label={`Delete ${plan.itinerary.title}`}>×</button></div><button className="library-open" onClick={() => onLoad(plan.itinerary)}><p>{new Date(plan.createdAt).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' })}</p><h3>{plan.itinerary.title}</h3><span>{plan.itinerary.days.length} days · {plan.itinerary.days.reduce((total, day) => total + day.stops.length, 0)} stops</span><b>Open route <i>→</i></b></button></article>)}</div></section>;
}
